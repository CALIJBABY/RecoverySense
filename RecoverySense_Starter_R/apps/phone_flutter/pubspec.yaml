name: recoverysense_phone
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

# Add these after Firebase setup:
# firebase_core: ^3.6.0
# firebase_auth: ^5.3.1
# cloud_firestore: ^5.4.4
# firebase_messaging: ^15.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
