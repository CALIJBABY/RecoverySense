# RecoverySense Flutter phone app

This folder is now a real Flutter app folder with the platform files Firebase expects.

Start here:

```bash
flutter pub get
flutter run
```

Then follow `FIREBASE_CONNECT_STEPS.md`.

Current status:

- Mock login works without Firebase.
- Dashboard uses mock sensor data.
- EMA screen saves to the mock repository.
- Firebase connection is next.
