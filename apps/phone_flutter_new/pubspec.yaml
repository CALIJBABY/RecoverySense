name: recoverysense_phone
description: RecoverySense phone application prototype.
publish_to: "none"
version: 0.5.3+10

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  archive: ^4.0.9
  cloud_firestore: ^6.7.1
  firebase_auth: ^6.5.6
  firebase_core: ^4.12.1
  flutter:
    sdk: flutter
  share_plus: ^11.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
